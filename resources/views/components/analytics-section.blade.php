@props(['label'])
<div class="flex items-center gap-3 pt-2">
    <span class="text-xs font-semibold uppercase tracking-widest text-slate-400">{{ $label }}</span>
    <div class="flex-1 h-px bg-slate-200"></div>
</div>
